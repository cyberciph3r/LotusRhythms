import React from "react";
import ReactDOM from "react-dom/client";
import RoutesCompo from "./RoutesCompo";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <RoutesCompo />
  </React.StrictMode>
);
